# 进度与交接日志

> 每次结束一段工作前更新本文件并推送。前面几节始终反映最新状态；工作日志按时间追加在末尾。

## 当前状态

- 阶段：M2 AI 改计划（功能、自动验证与开发评测完成；剩真机目视检查与合并）
- 最后更新：2026-09-24，由 Claude Code
- 当前负责方：Claude Code；工作分支 `claude/m2-ai-plan`

## 待用户确认

- [ ] 宠物在高缩放下显得偏小｜建议精灵尺寸档位按「整数倍缩放」随系统缩放放大（200% 时中档显示为 256 物理像素），仍然只用整数倍，像素保持清晰；需要改 `docs/decisions/0001` 的「物理像素为基准」约定并重跑像素门禁｜不改则在 125% 以上的屏幕上猫和清单比例失调，只影响观感

格式：`- [ ] 问题｜建议｜不确认的影响`

## 下一步

- M2 工作都在 `claude/m2-ai-plan`，完成后经 PR 合回 `from-scratch`。
- 真机目视检查新界面（见「需人工验收 · AI 改计划目视检查」），然后开 PR 合回 `from-scratch`。

## 已知问题

- 当前 Codex 会话的 Computer Use 只暴露浏览器能力，没有 Windows 原生应用枚举、启动或窗口绑定接口；无法用它打开/输入/观察记事本，后续原生窗口目视检查需换有 Windows 控制能力的会话或由用户手工检查。
- Desktop Cat 是开发期统一占位素材；按用户决定，悬停和拖动复用待机素材并采用离散像素位移动画。v1 发布前仍需补画或替换为 6/6 独立逐帧动画。
- 一键隐藏快捷键当前固定为 `Ctrl+Alt+H`；快捷键修改入口不在本次明确验收清单内，后续设置完善时实现。「跟宠物说」`Ctrl+Alt+空格` 同样暂不可改。
- 宠物精灵按 M1 设计使用固定物理像素档位（96 / 128 / 160 px），清单和对话框现在随系统缩放放大；在 200% 屏幕上猫会明显小于清单。是否让精灵档位也随缩放放大待用户决定（见「待用户确认」）。
- 豆包预设 `doubao-seed-2-1-lite-260915` 未达评测参考标准（e04 一次严重错误），且较慢（p90 17 秒，70 次中 4 次超过 30 秒超时）。用户 2026-09-24 决定保留。PRD 18.2 / 19 写的是「预设推荐模型达到参考标准」，这里是用户明确同意的例外。
- 在 WSL 里通过 `\\wsl.localhost\...` 路径运行 `verify-system.ps1` 时，单实例检查的 `Start-Process` 会被 Windows 的网络位置安全检查挡住而卡住。代理在 WSL 中工作时，应把仓库同步到 Windows 本地目录（如 `%LOCALAPPDATA%\Temp\roost-claude-work`）再运行桌面验证；在 Windows 本地克隆里直接运行不受影响。
- 验证器没有 `Application.Run` 主循环，模态窗口关闭后 WinForms 会卸载同步上下文；`Roost.Verify` 在每次发送前重新安装。真实应用有主循环，不受影响。

## 验收状态

对应 PRD 第 18.2 节。状态取值：未开始 / 通过 / 失败 / 不适用。

| 模块 | 所属阶段 | 状态 | 验证方式（命令或脚本路径） | 最近一次结果 |
|---|---|---|---|---|
| 点击穿透 | M1 | 通过 | `.\verify-system.ps1`；真实缩放用 `.\verify-dpi.ps1 -ExpectedScalePercent <100\|125\|150>` | 100% / 125% / 150% 三档均检测到预期实际缩放；每档 12/12 空白点击落到下层，宠物区域命中，`overallPass: true` |
| 窗口排版 | M2 | 通过（200%） | `.\verify-layout.ps1`；真实缩放用 `.\verify-dpi.ps1 -ExpectedScalePercent <100\|125\|150>` | 200%：设置 4 个标签页、编辑待办、AI 预览、模型测试题、清单共 8 处，0 处文字放不下 / 重叠 / 越界。100% / 125% / 150% 待用户切换缩放后复测 |
| 像素清晰 | M1 | 通过 | `.\verify-pixel.ps1`；真实缩放用 `.\verify-dpi.ps1 -ExpectedScalePercent <100\|125\|150>` | 三档各有 18/18 真实窗体截图及 54/54 模拟布局通过，异常颜色像素 0，`overallPass: true` |
| 资源占用 | M1 | 通过 | `.\verify-resource.ps1` | 600.001 秒、4,792 帧：平均 CPU 0.1701%，平均/峰值内存 54.06/55.17 MB；隐藏 30.021 秒 0 帧。PRD 18.2 的 1 小时验收留到 M4 |
| 清单与日期规则 | M1 | 通过 | `.\test.ps1` | 排序、今日筛选、5 条折叠、03:59/04:00 边界通过 |
| 待办操作 | M1 | 通过 | `.\test.ps1` | 只填标题新建、编辑、完成/取消、软删除/撤销、星标及重启持久化通过 |
| 桌面系统集成 | M1 | 通过 | `.\verify-system.ps1` | 托盘项、全局快捷键、隐藏暂停、全屏隐藏/恢复、单实例唤回通过 |
| 布局与设置 | M1 | 通过 | `.\test.ps1` | 位置持久化、可见区域找回、清单翻边、贴边、透明度下限、点击/拖动阈值通过 |
| 离线 | M1 | 通过 | `.\test.ps1` | 核心与持久化测试不使用网络；应用不配置模型也可完成全部待办操作 |
| 数据安全 | M1 | 通过 | `.\test.ps1` | 原子提交前强杀子进程后主文件完整；连续保存仅保留最新 7 份备份 |
| 隐私 | M1 起 | 通过 | 代码检查 + `.\test.ps1` | 应用没有日志模块；错误提示不含待办内容；数据仅写入本地数据文件与备份 |
| AI | M2 | 通过（豆包例外） | 开发评测：`.\eval-ai.ps1 -Preset <预设>`；判分自检与模型测试题：`.\test.ps1`、`.\verify-ai.ps1` | 评测集与判分自检通过（完美模型 70/70；故意犯错时查出 15 个致命错误）；模型测试题 16 题可解，窗口端到端跑通且只发送虚构待办。开发评测（`docs/evidence/m2/`）：DeepSeek、千问、智谱 70/70 且 0 严重错误；豆包 69/70、1 次严重错误（e04），用户决定保留为预设并接受 |
| AI 确认 | M2 | 通过 | `.\verify-ai.ps1` + `.\test.ps1` | 真实宠物窗口 + 本地假模型 13/13：预览期间与取消后数据文件逐字节不变，确认后整批生效、整批撤销；失败路径均保留原文且不改数据 |
| 导出 | M4 | 未开始 | | |

### 需人工验收

无法自动化的检查列在这里，写明手动操作步骤和预期结果。

### Computer Use 原生窗口能力

1. 在当前 Codex 会话请求原生应用清单并尝试启动 `notepad.exe`。
2. 预期：能唯一绑定记事本窗口、输入一行虚构文字并从窗口状态或截图看到结果。
3. 本次结果：失败。运行时只暴露浏览器接口，`listApps` / `computer.launch_app` 不存在；未能打开或控制记事本。

### 真实 DPI 复测（已通过）

1. 保存工作并关闭正在运行的 Roost 和其他会遮挡测试区域的置顶窗口；在 Windows 设置中把单显示器缩放设为 100%，注销/重启按系统提示执行。
2. 在仓库根目录运行 `.\verify-dpi.ps1 -ExpectedScalePercent 100`。脚本会拒绝实际 DPI 不匹配的结果，并把证据写入 `artifacts\m1\dpi-100\`。
3. 将缩放改为 125%，运行 `.\verify-dpi.ps1 -ExpectedScalePercent 125`；再改为 150%，运行 `.\verify-dpi.ps1 -ExpectedScalePercent 150`。
4. 2026-09-22 实测结果：三档均输出 `PASS real Windows scale ...`；系统验证每档 12/12 空白点击通过，像素验证每档 18/18 真实窗体截图与 54/54 布局用例通过，异常颜色像素均为 0。
5. 证据已归档到 `docs/evidence/m1/dpi-100/`、`dpi-125/`、`dpi-150/`。自动脚本不会修改系统缩放，也不会把模拟布局冒充实机覆盖。

### AI 改计划目视检查（M2，待评测通过后执行）

1. 在设置「AI 与自启」选一个已通过评测的预设，填 key 后点「保存并检测」；预期首次出现隐私告知，确认后显示「检测通过，已保存」。故意填错 key 再保存，预期当场提示 key 无效且不保存。
2. 在清单的「跟宠物说」输入「明天下午三点复盘会很重要，另外把一条虚构待办删掉」；预期宠物进入思考状态，预览里删除排在最上面并标红，新增显示具体日期「9月X日 星期X 15:00（明天）★」。
3. 去掉删除那条的勾选后确认；预期只新增一条，清单下方出现「撤销这次改动」，点击后新增消失。
4. 按 `Ctrl+Alt+空格`，预期宠物出现、输入框获得焦点。断开网络后发送一句话，预期气泡提示连不上且原文还在输入框里。
5. 在设置「AI 与自启」点「用测试题检验…」，开始测试；预期说明会产生少量费用，进度逐题前进，可取消；结束后显示「答对 X / 16 题」和错题（你说 / 应当 / 模型 / 问题），出现严重错误时红字提醒但不影响继续使用。

### 首次启动与日常交互目视检查

1. 指定一个新的空数据目录运行 `dist\Roost.exe`；预期出现空清单引导，明确说明「+」、点击宠物折叠、AI 与自启设置入口，且不强制配置模型。
2. 通过表单新建、编辑、星标、完成后取消、删除后撤销一条虚构待办；预期 UI 与自动测试中的状态变化一致，完成时播放庆祝状态。
3. 拖动宠物到四条屏幕边缘；预期超过阈值才移动、靠边吸附，清单在空间不足时翻到另一侧，重启后位置保持。
4. 右键托盘依次检查显示/隐藏、设置、退出；打开一个真正的全屏视频或演示，预期宠物隐藏并在退出全屏后恢复。

## 工作日志

### 2026-09-19 Claude Code

- 建立协作文件：`AGENTS.md`、`CLAUDE.md`、本文件、`docs/decisions/0000-template.md`。

### 2026-09-20 Codex

- 先执行 `git pull --ff-only`，确认仓库已是最新。
- Computer Use 冒烟失败：当前宿主没有 Windows 原生应用控制接口，无法打开和输入记事本；已记录为目视检查能力限制。
- 比较 .NET Framework WinForms + Win32、Tauri 2 + WebView2、原生 C++ Win32 三个候选；新增提议决策 `docs/decisions/0001-tech-stack.md`。
- 在 `spikes/` 新增可重复编译和运行的三项自动验证。无需安装 SDK，使用系统 .NET Framework 编译器。
- 点击穿透：跨进程下层窗口在 100% / 150% 模拟几何场景各收到 12/12 次空白点击，置顶精灵区域各收到 2/2 次点击；功能通过，真实指定 DPI 覆盖不足。
- 像素清晰：100% / 125% / 150% 布局 × 2× / 3× / 4× 尺寸共 9 张截图，异常颜色和异常像素均为 0；功能通过，真实指定 DPI 覆盖不足。
- 资源：8 FPS 播放 600.005 秒，平均 CPU 0.1795%，平均/峰值 Working Set 37.93/39.80 MB；隐藏 30.042 秒期间 0 帧，CPU 0.0098%。
- 核对 3 个开源素材候选的来源、许可、再分发、署名和动画覆盖，记录在 `docs/asset-candidates.md`；没有候选原样达到 6/6。
- 没有修改 PRD，也没有开始 M1。

### 2026-09-20 Codex（M0 收尾）

- 用户确认采纳 C# / .NET Framework 4.8 + Windows Forms + Win32 技术栈；`docs/decisions/0001-tech-stack.md` 状态改为“已采纳”。
- 用户选择 MIT Desktop Cat 作为开发期占位素材，允许悬停和拖动临时复用近义动画，未来再补画或替换。
- 按用户决定删除 PRD 的多显示器支持与验收要求；真实 100% / 125% / 150% 缩放复测延期到 M1 完成后，并作为进入 M2 前的门禁。
- M0 依据三项可重复运行的功能 spike、10 分钟资源数据、已采纳技术栈和已选占位素材完成收尾；未把尚未执行的真实缩放与 1 小时资源验收记为通过。
- 没有开始 M1。

### 2026-09-21 Codex（M1 实现）

- 从 `from-scratch` 新建并推送 `codex/m1-desktop-todos`；所有 M1 工作都在该分支完成，未修改 PRD。
- 实现 .NET Framework 4.8 WinForms 产品：透明置顶像素宠物、按素材 alpha 建立点击区域、三档尺寸、离散动画、拖动/点击区分、贴边、位置找回与记忆、清单翻边、透明度下限。
- 实现托盘显示/隐藏/设置/退出、`Ctrl+Alt+H` 全局隐藏、全屏自动隐藏与恢复、屏幕锁定暂停、单实例及重复启动唤回。
- 实现本地待办：新建、编辑、完成及取消、软删除及撤销、星标、三组排序、只显示今日、5 条折叠、04:00 一天起点、相对时间标签、空清单与首次启动引导。
- 实现 UTF-8 JSON 原子替换、崩溃恢复和最近 7 份备份；没有日志或网络依赖。
- 导入已选 Desktop Cat 素材并固定到上游提交 `c9b433f5a11f67e2016b6f6f4d45b26b1963ab14`，同时保存 MIT 许可证和仓库署名。
- 自动验证：核心 10 项通过；桌面系统集成与单实例通过；像素 54/54 通过；资源 600.001 秒平均 CPU 0.1701%、平均/峰值内存 54.06/55.17 MB、隐藏 30.021 秒 0 帧。
- 真实 100% / 125% / 150% Windows 缩放仍需用户人工切换后执行产品级脚本，因此暂未把 M1 标记为最终完成，也未开始 M2。

### 2026-09-22 Codex（PowerShell 5.1 兼容性修复）

- 用户在真实 100% 缩放复测时发现 `verify-dpi.ps1` 被 Windows PowerShell 5.1 按本地代码页读取，中文字符串乱码并造成解析错误。
- 将仓库全部 PowerShell 脚本统一为带 BOM 的 UTF-8；新增 `verify-powershell.ps1`，自动检查所有脚本的 BOM 和语法，`test.ps1` 会先运行该检查。
- 已使用 Windows PowerShell 5.1 验证：14 个脚本编码与语法通过，核心测试 10/10 通过，当前机器 200% 真实缩放门禁通过。
- 此问题属于验证入口兼容性，不是点击穿透或像素检查失败；修复后仍需重新执行 100% / 125% / 150% 实机门禁。

### 2026-09-22 Codex（M1 收尾）

- 核对用户完成的 100% / 125% / 150% 真实 Windows 缩放门禁：三个 `system.json` 和三个 `pixel.json` 均检测到预期实际缩放，`overallPass` 均为 `true`。
- 点击穿透每档 12/12 空白点击落到下层，宠物区域命中；像素检查每档 18/18 真实窗体截图与 54/54 模拟布局通过，异常颜色像素均为 0。
- 将三档 JSON 与各 18 张真实缩放截图归档到 `docs/evidence/m1/`，完成此前延期的合并门禁。
- M1 全部自动验收项通过，阶段状态改为已完成；没有开始 M2。

### 2026-09-23 Claude Code（接手）

- 执行 `git fetch`，确认本地 `from-scratch` 与远端一致，M1 已由 PR #2（`codex/m1-desktop-todos`）合入。
- 从 Codex 接手开发；用户要求开始 M2。本条仅更新交接状态，未改动代码与 PRD。

### 2026-09-23 Claude Code（M2 实现）

- 从 `from-scratch` 新建并推送 `claude/m2-ai-plan`；未修改 PRD。
- 实测 csc 编译程序的 HTTPS：默认只启用 SSL3/TLS1.0，智谱、豆包握手失败；显式开启 TLS 1.2/1.3 后五家都返回 401。数据写入提议决策 `docs/decisions/0002-ai-plan-pipeline.md`。
- Core：发送范围（未完成 + 7 天内完成，短别名）、按一天起点生成的 14 天日期表与提示词、回复解析与本地校验（找不到的待办、无变化、重复、同句已删除、格式错误均判无效；过去时间警示）、人话摘要、确认后整批写入与整批撤销；OpenAI 兼容客户端（30 秒超时、取消、失败分类，401 不回显厂商原文以免带出 key）；Windows 凭据管理器存 key；五家预设。
- 界面：清单上的「跟宠物说」输入框与 `Ctrl+Alt+空格`；思考状态；宠物气泡；预览确认窗口（删除置顶标红、逐条去勾、无效条目禁用）；「撤销这次改动」；设置页 AI 配置（预设、保存即检测、首次隐私告知）；宠物状态按 PRD 6.3 优先级统一决定。
- AI 评测集 `tests/ai-eval/cases.json`：70 句虚构语料，覆盖新增、修改、完成、取消完成、星标、删除、歧义、没听懂、不存在、组合、不误伤、03:59/04:00 边界；`Roost.Eval.exe` 评分并单独统计三类致命错误；`eval-ai.ps1` 供用户用真实 key 运行。
- 验证：`.\test.ps1` 19/19 + 评测自检通过；`.\verify-ai.ps1` 13/13；`.\verify-system.ps1`（空白点击 12/12、单实例）与 `.\verify-pixel.ps1`（54/54 + 18/18）在改动清单布局后仍通过。
- `.\verify-resource.ps1`：600.011 秒平均 CPU 0.1997%，平均/峰值内存 51.77/53.38 MB，隐藏 30.027 秒 0 帧，通过。但可见期间只有 1,984 帧（M1 为 4,792）。用 M1 提交 `3017469` 与当前版本各跑两轮 30 秒对照，两版都是 240 帧、8.00 FPS，排除代码回退；推测测试期间屏幕锁定或息屏触发了按设计的动画暂停，未能事后证实。对照中当前版本 CPU 略高（0.2962% 对 0.2409%，0.2181% 对 0.1855%），仍远低于 0.5%。
- 调试验证器时被强杀的进程在凭据管理器留下 7 条 `Roost/Verify/*` 假 key，已用 `cmdkey` 清除。
- 停在需要用户提供 key 与确认预设的节点，没有用真实模型跑评测。

### 2026-09-24 Claude Code（temperature 兼容）

- 用户评测时发现 `kimi-k2.6` 只接受 `temperature` 为 1，默认的 0 被拒绝。按用户要求：请求被 400 拒绝且返回内容提到 `temperature` 时，去掉该参数重试一次；其他厂商仍用 0，其他 400 不重试，最多重试一次。
- 验证：`.\test.ps1` 20/20（新增用例覆盖：拒绝后重试成功且第二次请求不含 temperature；连续拒绝只重试一次；无关的 400 不重试）+ 评测自检通过；`.\verify-ai.ps1` 通过。未用真实 Kimi key 复测。

### 2026-09-24 Claude Code（预设调整与模型测试题）

- 按用户决定：预设去掉 Kimi；千问改为 `qwen3.8-flash`；豆包改为 Doubao-Seed-2.1-lite（`doubao-seed-2-1-lite-260915`，ID 取自火山方舟模型列表）。
- 用户批准修改 PRD：评测集定位为开发期挑选预设的参考，不限制用户选模型（8.6、9.2）；新增 9.6「模型测试题」；12、18.1、18.2、19 同步。
- 评测判分移入 Core `AiEvalSuite`，开发评测与应用内测试题共用同一套规则；语料移到 `assets/ai-eval/cases.json` 随应用发布，其中 16 题标记为测试题（覆盖新增、修改、完成、删除、歧义、没听懂、不存在、不误伤与一天起点）。
- 设置页新增「用测试题检验…」：只用虚构待办、可取消、显示得分与错题，严重错误红字提醒但不限制使用。
- 验证：`.\test.ps1` 22/22 + 自检（70/70；故意犯错 15 个致命错误；测试题 16/16 可解）；`.\verify-ai.ps1` 14 项通过，新增项确认测试题窗口发出 16 次请求、摘要正确、请求中不含用户自己的待办。设置页与测试窗口尚未在真机上目视检查。

### 2026-09-24 Claude Code（开发评测结果）

- 用户跑完四个预设的开发评测，报告归档到 `docs/evidence/m2/`（已检查不含 key，只有虚构数据）：
  - DeepSeek `deepseek-flash`：70/70，0 严重错误，中位数 / p90 / 最慢 882 / 1378 / 1982 ms。
  - 通义千问 `qwen3.8-flash`：70/70，0 严重错误，3005 / 4564 / 13526 ms。
  - 智谱 `glm-5.3-flash`：70/70，0 严重错误，4636 / 8471 / 65859 ms（70 次中 1 次超过 30 秒）。
  - 豆包 `doubao-seed-2-1-lite-260915`：69/70，1 次严重错误（e04「旧任务标记完成」被执行成完成「报销差旅费」），7899 / 17224 / 82924 ms（70 次中 4 次超过 30 秒）。
- 用户确认三个默认值：「跟宠物说」`Ctrl+Alt+空格`；AI「撤销这次改动」显示 10 秒；AI 失败气泡 15 秒后自动收起。
- 用户决定：豆包 lite 保留为预设、接受 e04 失败；AI 请求超时保持 30 秒；决策 0002 改为「已采纳」，并补入评测与延迟数据。
- 补充目视检查（不代替真机检查）：用 `DrawToBitmap` 在 96 DPI 下渲染设置页 AI 标签、预览窗口（新增 / 过去时间警示 / 完成 / 删除 / 无效各一条）、测试题窗口，未见文字截断或控件重叠；无 key 时「用测试题检验…」为禁用状态。真机 200% 下的交互检查仍待用户按「AI 改计划目视检查」执行。

### 2026-09-25 Claude Code（高缩放下排版修复）

- 用户在真机 200% 缩放下发现设置页文字大量重叠。原因：进程是 PMv2 DPI 感知，字体按点数随缩放放大，但界面坐标是写死的 96 DPI 像素，没有跟着放大。设置页「外观」自 M1 起即如此；编辑待办、AI 预览、模型测试题、清单同样受影响。此前的 `DrawToBitmap` 补充检查在 96 DPI 下渲染，没能发现。
- 新增 `Roost.Verify layout` / `.\verify-layout.ps1`：在当前真实缩放下打开每个窗口，检查文字放得下、同级控件不重叠、控件不超出父容器，并保存截图；已接入 `verify-dpi.ps1` 与 `verify-all.ps1`。修复前在 200% 下报出 40 余处问题。
- 修复：新增 `DpiScale`，对话框按 96 DPI 坐标建好后整体放大一次；清单面板、每行待办、空清单提示和宠物气泡同样按缩放放大；清单尺寸与间距参与布局前按缩放换算。曾先尝试 WinForms 自带的 `AutoScaleMode.Dpi`，部分控件被放大两次，放弃。宠物精灵保持 M1 的物理像素档位不变。
- 验证（200%）：`.\verify-layout.ps1` 8 处 0 问题，截图人工看过；`.\test.ps1` 22/22 + 自检；`.\verify-ai.ps1` 通过；`verify-dpi.ps1 -ExpectedScalePercent 200` 中点击穿透 12/12、像素、排版通过，但「全局快捷键已注册」失败——用户正在运行的旧版 Roost（`\\wsl.localhost\...\dist\Roost.exe`）占用了 `Ctrl+Alt+H`；单实例检查同理无法进行。代理未结束用户的进程，待用户退出 Roost 后重跑 `verify-system.ps1` 与 `verify-dpi.ps1`。
