# Roost

一只住在 Windows 桌面上的像素小宠物，旁边挂着待办清单，可以用一句话让 AI 改计划。自用项目，将来可能开源。

## 必读

- `docs/PRD.md`：**唯一的需求来源**。功能、数值、取舍都以它为准。
- `docs/PROGRESS.md`：当前进度与交接日志。**开始工作前先读。**
- `docs/decisions/`：已做出的技术决策。

## 协作方式

本仓库由多个 AI 代理（Codex、Claude Code）轮流在同一分支上开发，通过 GitHub 同步。代理之间的对话记录不互通，**仓库里的文件是唯一的交接渠道**。

- 同一时间只有一方在工作。开始前先 `git pull`。
- 每完成一个可验证的小步就提交并推送。提交信息写明做了什么、怎么验证的。
- 结束一段工作前，更新 `docs/PROGRESS.md` 并推送。
- 不强推，不改写已推送的历史。

## 需求与决策

- **不要自行修改 PRD，除非用户明确批准。** 遇到以下情况，写进 `docs/PROGRESS.md` 的「待用户确认」：
  - 需求不清楚；
  - PRD 前后矛盾；
  - 实测发现某项需求做不到。

  写明问题、你的建议、不确认会有什么影响。能绕开的先做其他部分，绕不开就停下。
- 技术选型等会影响后续开发的决策，按 `docs/decisions/0000-template.md` 写一份记录，编号递增。决策必须附实测数据，不能只凭经验判断。
- 新写的决策状态为「提议」，用户确认后才改为「已采纳」。

## 代码规范

- 先读周边代码再写，跟随已有风格，不引入项目里没在用的写法。
- 改动最小化，不顺手重构无关代码。
- 新增依赖要说明理由。PRD 第 13 节对安装包体积、内存、CPU 有明确要求。

## 构建与运行

开发与测试都在 Windows 本机进行。技术选型完成后，在这里补充构建、运行、测试的命令。

## 验证

- 写完代码不等于功能完成，以 PRD 第 18.2 节的验收标准为准。
- 能自动化的检查，必须写成可以重复运行的测试或脚本，并在 `docs/PROGRESS.md` 的验收状态表里注明运行方式。
- 截图或 computer use 的目视检查只能作为补充，不能代替自动化检查。
- 检查不通过时，修代码，而不是放宽检查。

## 隐私红线（PRD 第 11 节）

- 待办内容不得出现在任何日志或错误信息里。
- API Key 只存放在系统安全存储（Windows 凭据管理器）中，不写入任何文件、日志，也不提交进仓库。
- 测试只使用虚构数据，不提交任何真实的 key 或个人数据。
